# 2026-W28 工程日志（07-07 ~ ）

## `<foreignObject>` 规范违规清理（07-15 完成）

**目标**：将 11 个文件中 14 处 SVG 内 `<foreignObject>` 全部迁移至 HTML 层，消除 SVG/HTML 两套缩放叠加导致的图表异常风险。

**涉及文件**（11 个）：
- **高优先级**（6 个）：`ProjectileAnimation.tsx`、`ObliqueThrowAnimation.tsx`、`GravityAnimation.tsx`、`SatelliteAnimation.tsx`、`MomentumScene.tsx` + `MomentumAnimation.tsx`、`ACGeneration.tsx`
- **中优先级**（4 个）：`WorkVTChart.tsx` + `WorkAnimation.tsx`、`VerticalThrowCharts.tsx` + `VerticalThrowAnimation.tsx`、`EnergyTimeChart.tsx` + `PotentialEnergyAnimation.tsx`、`FaradayChartPanel.tsx` + `FaradayLaw.tsx`
- **低优先级**（1 个）：`ForceDecompositionCard.tsx` + `VerticalCircularAnimation.tsx`

**迁移模式**：
- 画中画图表 → HTML `absolute` 浮层（保留原有位置与尺寸）
- 专用图表面板 → 子组件移除 `foreignObject` 外壳，父级改为 HTML 层布局
- 拖拽交互同步重构（`handleChartMouseDown` 等从 SVG 事件改为 HTML div 事件）

**验证**：TypeScript 0 errors，Vitest 701 passed，ESLint 0 errors 0 warnings

---

## 左屏控制台 SidebarExtra 全量清理（07-13 完成）

**目标**：将所有手写 SidebarExtra 收敛为声明式 controlMeta，消除左屏维护碎片化。

**最终结果**：
- SidebarExtra 从 61 → 0（全部清理完成）
- 剩余 2 个硬骨头（ACValues / PowerTransmission）在更早的 refactor 中已解决
- controlMeta 协议覆盖所有标准控件类型（number/segmented/toggle/preset/tip）

**涉及批次**（汇总自 07-01 ~ 07-13）：
- 第一批（07-01）：8 批次，32 个动画迁移（24 完全删除 + 8 部分精简）
- P0（07-02）：action + storeToggle 类型扩展，4 个部分精简
- P1（07-02）：preset 函数式 params，5 个部分精简
- P1.5（07-02）：左屏 0-6 顺序规范，4 个完全删除
- P2（07-02）：onChangeSideEffect 联动
- 后续清理（~07-13）：剩余条目逐步收敛

**验证**：tsc 零错误，build/test 通过

---

## 力方向纯函数扩展 P1 迁移（07-11 完成）

**目标**：将复合场页面因 SVG/物理坐标系混淆导致力方向反转 bug 修复后的纯函数调用扩展到更多电磁学场景。

**新增 helper**（`src/physics/magnetism/forces.ts`）：
- `lorentzForceDir` — 带电粒子磁场偏转
- `electricForceDir` — 电场对电荷作用力
- `centripetalForceDir` — 圆周运动向心力

共 21 个单测。

**P1 迁移完成清单**：

| 文件 | 场景 | 日期 |
|------|------|------|
| `velocitySelector.ts` | 洛伦兹力方向 | 2026-07-11 |
| `SimulationView.tsx`（BoundaryMagneticField） | 双边界磁场出射矢量方向 | 2026-07-11 |
| `ChargeInEField.tsx` | 电场力方向 | 2026-07-11 |

**验证**：tsc --noEmit + 电磁学领域相关测试通过

---

## Viewport 架构迁移状态核对（07-13）

**当前进度**：COMPLIANT ~95 个 / LEGACY 动画页面 3 个

| 文件 | 风险 | 说明 |
|------|------|------|
| `SatelliteAnimation.tsx` | MEDIUM | useCanvasSize(CANVAS_PRESETS.full) + useViewport |
| `SpringCompositeAnimation.tsx` | MEDIUM | useCanvasSize({700,650}) |
| `StroboscopicAnimation.tsx` | LOW | useCanvasSize({400,180})，小尺寸自定义 viewBox |

**审计缺陷**：11/11 已修复（viewport_audit_report 二次审计清零）

**说明**：CenterExtra / Chart / Sidebar / 子组件使用 useCanvasSize 属正常模式，不计入 LEGACY。OrbitTransferAnimation、CentripetalAnimation、KeplerAnimation 等已迁移到 useAnimationViewport。

---

## 粒子轨迹统一渲染迁移 — CircularGeometryModel（07-13）

**目标**：将 `CircularGeometryModel.tsx` 中自定义 Canvas 拖尾+粒子本体渲染迁移到标准 `drawCanvasParticleTrajectory` 组件。

**改动**：
- `CircularGeometryModel.tsx`：导入 `drawCanvasParticleTrajectory`，新增 `historyPoints`（完整路径采样）和 `tailPoints`（最近 8 点拖尾）两个 `useMemo`，替换原 40 行自定义 Canvas 绘图逻辑为标准组件调用
- 粒子历史轨迹完整保留（从 t=0 起的全路径虚线 + 短拖尾渐变 + 本体球）
- 矢量箭头与几何标注（SVG Overlay）保持不变

**验证**：tsc 零错误，44 个测试文件 / 638 测试全通过
