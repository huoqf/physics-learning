# 2026-W27 工程日志（07-01 ~ ）

## SidebarExtra → controlMeta 迁移（07-01 ~ 07-02）

**目标**：将手写 SidebarExtra 中的标准控件提取到声明式 controlMeta，减少左屏维护成本。

**最终结果**：
- 完全删除：39 个（原始 61 → 剩 22）
- 硬骨头：2 个（ACValues / PowerTransmission）
- tsc 零错误

---

### 第一批（07-01）：基础控件迁移

**迁移批次**：

| 批次 | 领域 | 动画数 | 方式 |
|------|------|--------|------|
| A | mechanics-momentum | 6 | 完全迁移 |
| B | mechanics-dynamics | 3 | 完全迁移 |
| C | mechanics-circular | 2 | 完全迁移 |
| D | electromagnetism-electrostatics | 3 | 完全迁移 |
| E | electromagnetism-magnetism | 2 | 完全迁移 |
| F | thermodynamics + ac + dc-circuits | 5 | 完全迁移 |
| G | dynamics + kinematics + dc | 6 | 部分迁移 |
| H | optics + second-law | 4完全+1部分 | 混合 |

**完全迁移清单**（24 个）：
momentum: anim-momentum, anim-impulse-concept, anim-impulse, anim-momentum-conservation, anim-collision, anim-curved-slot
dynamics: anim-weightlessness, anim-newton-second, anim-connected-bodies
circular: anim-circular-motion, anim-centripetal
electrostatics: anim-electric-field, anim-charge-in-efield, anim-capacitor
magnetism: anim-ampere-force, anim-charge-in-bfield
thermodynamics: anim-brownian-motion, anim-intermolecular-forces, anim-clapeyron
ac: anim-transformer
dc: anim-ohm-law
optics: anim-thin-lens, anim-total-reflection, anim-refraction, anim-reflection

**部分精简清单**（8 个）：
anim-gravity-basic, anim-gravity, anim-friction, anim-kinematics-advanced, anim-circuit-analysis, anim-closed-circuit, anim-second-law, anim-coulomb-law/anim-kepler

---

### P0（07-02）：action + storeToggle 类型

**改动**：`types.ts` 新增 action/storeToggle；`ControlPanel.tsx` 新增渲染逻辑；`AnimationPage.tsx` 传入 toggleVectors/setDirection
**迁移**：KinematicsAdvancedSidebar, FrictionSidebar, VelocitySelectorSidebar, AccelerationSidebar（4 个部分精简）

### P1（07-02）：preset 函数式 params

**改动**：`types.ts` preset.params 支持函数；`ControlPanel.tsx` handlePresetApply 检测函数类型
**迁移**：GasLawsSidebar, FirstLawSidebar, SpringCompositeSidebar, ElectricPotentialSidebar, UniformAccelerationSidebar（5 个部分精简）

### P1.5（07-02）：左屏 0-6 顺序规范

**改动**：`AnimationPage.tsx` 拆分 ControlPanel 为 modeControls + auxControls
**迁移**：AccelerationSidebar, VelocitySelectorSidebar, FrictionSidebar, GasLawsSidebar（4 个完全删除）

### P2（07-02）：onChangeSideEffect 联动

**改动**：`types.ts` ControlCondition 新增 onChangeSideEffect；`ControlPanel.tsx` 新增 applySideEffect
**迁移**：FirstLawSidebar, SpringCompositeSidebar, ElectricPotentialSidebar（3 个完全删除）

### P3（07-02）：tip 动态 content

**改动**：`types.ts` tip.content 支持函数；`ControlPanel.tsx` 渲染时解析
**迁移**：LenzsLawSidebarExtra（1 个完全删除）

### P4（07-02）：电磁感应自定义布局

**改动**：`electromagnetism-induction.ts` 添加 controlMeta
**迁移**：InductionSidebarExtra, CuttingEMFSidebarExtra, FaradaySidebarExtra（3 个完全删除）+ LenzsLaw（P3 已删）

### P5（07-02）：人船模型 left-panel 规范化

**改动**：`mechanics-momentum.ts` 将 ManBoatSidebar 键盘操作提示迁移为 `controlMeta` tip；删除 `ManBoatSidebar.tsx`；修复预设文案重复"依次依次运动"→"依次运动"
**迁移**：ManBoatSidebar（1 个完全删除）

---

## 超长文件拆分（07-01 ~ 07-02）

| 文件 | 原行数 | 拆分结果 | 完成日期 |
|------|-----:|---------|---------|
| `ForceMotionSandbox.tsx` | 758 | → `hooks/useForceMotionSandbox.ts`(253) + 组件(331) | 07-01 |
| `EquilibriumAnimation.tsx` | 737 | → `hooks/useEquilibriumLayout.ts`(165) + 组件(430) | 07-01 |
| `CircuitAnalysis.tsx` | 507 | → `physics/dcCircuit.ts`(calculateCircuitAnalysis) + `model/circuitAnalysisLayout.ts` + 组件(~370) | 07-02 |
| `ObliqueThrowAnimation.tsx` | 589 | → `useObliqueThrowLayout.tsx`(134) + 组件(344) | 07-02 |

## 左屏控制台迁移完成项（07-01 ~ 07-02）

- P1：全局增强 `ParamControl`（精确输入、格式化、零点标记、恢复默认）
- P1：统一左屏容器（`LeftPanel / LeftPanelSection / LeftPanelScrollArea`）
- P2：扩展参数协议（`ParamMeta` 增加 group/description/marks/importance/resetOnChange）
- P2：引入 `controlMeta`（支持 segmented/toggle/preset/tip/action/storeToggle）
- P2-P4：收敛 SidebarExtra（61→17，44 个已删除）
- FreeFallSidebar 迁移：模式切换/物体选择/重力场预设/时间切片→controlMeta，气压→paramMeta
- VelocitySidebar 迁移：观察模式/生活场景/Δt 步进/运动模型/教学提示→controlMeta，进阶 Δt→paramMeta，SidebarExtra 已删除
- VerticalThrowSidebar 迁移：观察模式/环境重力场预设→controlMeta，微元切片密度/空气阻力/目标高度线→paramMeta（showIf），"对比真空参考轨道"→controlMeta（showIf: airResistance），SidebarExtra 已删除
- ProjectileSidebar 迁移：全部→controlMeta + paramMeta，SidebarExtra 已删除
- ObliqueThrowSidebar 迁移：同 ProjectileSidebar，SidebarExtra 已删除

## 响应式规范清理（07-02 核查）

- B 类混合文件残留：已全部清理（0 处）
- 2.2 rgba()/fontSize：PowerTransmission 动态 rgba() 为合理使用（物理驱动颜色插值），VectorPlayground 为调试文件，均跳过

## CANVAS_PRESETS 废弃别名清理（07-02）

- 迁移 31 个组件：`standard`→`wide`(8)、`mediumTall`→`tall`(6)、`mediumWide`→`wide`(7)、`extraWide`→`wide`(9)、漏网 `useLightRodRopePhysics`(1)
- 同步更新 designWidth/designHeight：standard 高度 420→400、mediumTall 宽度 650→700、mediumWide 宽度 650→700、extraWide 宽高 800×440→700×400
- `spacing.ts` 删除 4 个废弃别名（standard/mediumTall/mediumWide/extraWide）及注释块
- ReflectionAnimation/ThinLensAnimation 的 preset 已迁为 `wide`，旧方案迁移（VIEW_WIDTH/VIEW_HEIGHT → 方式A）留待 §1.5.2
- tsc 零错误、385 tests 全部通过

## 旧方案组件迁移（07-02）

- `ReflectionAnimation`：移除 VIEW_WIDTH/VIEW_HEIGHT/REFLECTION_DESIGN 常量、移除 useViewport 调用及 vp.transform，SVG 改为静态 `viewBox="0 0 800 500" preserveAspectRatio="xMidYMid meet"`
- `ThinLensAnimation`：移除 VIEW_WIDTH/VIEW_HEIGHT 常量，viewBox 改为静态字面量 `"0 0 800 500"`，子组件 prop 传字面量
- tsc 零错误、385 tests 全部通过
