# 2026-W24 工程日志（06-05 ~ 06-15）

## 2026-06-15

### 颜色系统清理：删除迷你版 + 统一导入路径
**类型**：refactor | **范围**：theme, components, data

- 删除 `colors.ts` 中无消费者的迷你版 `PHYSICS_COLORS`（3 组：current/magnet/force）和迷你版 `SCENE_COLORS`（3 组：copper/rail/skin），共 35 行
- 统一 `Block.tsx`、`Ball.tsx` 导入路径从 `@/theme/physics/sceneColors` → `@/theme/physics`
- 统一 `forceMotion.ts` 导入路径从相对路径 `../../theme/physics/colors` → `@/theme/physics`
- 验证全项目 0 引用被删除代码，`tsc --noEmit` 零错误

**涉及文件**：`src/theme/colors.ts`(修改), `src/components/Physics/Block.tsx`(修改), `src/components/Physics/Ball.tsx`(修改), `src/data/quantities/forceMotion.ts`(修改)

**验证**：`npx tsc --noEmit` 零错误 · `grep` 确认 0 引用

### 颜色治理 Phase 1-2：tailwindColors/neutral[0]/CANVAS_COLORS
**类型**：refactor | **范围**：theme

- 删除 `tailwindColors` 死代码（Tailwind v4 用 CSS 变量，无需 JS 中间层）
- `neutral[0]` → `neutral.white`，17 个文件 64 处引用更新
- `CANVAS_COLORS` 8 个 neutral 映射色从硬编码 hex 改为 `colors.neutral[*]`

**涉及文件**：`src/theme/colors.ts`, `src/theme/index.ts`, 17 个 `.tsx` 文件

**验证**：`npx tsc --noEmit` 零错误

### 颜色硬编码清理（§2 完成）
**类型**：fix | **范围**：components/Physics

- `Block.tsx`：`#475569`×2 → `CANVAS_COLORS.labelTextLight`，`#FFFFFF` → `colors.neutral.white`
- `SportsCar.tsx`：`#EF4444` → `PHYSICS_COLORS.acceleration`，`#3B82F6` → `PHYSICS_COLORS.velocity`
- `LightBulb.tsx`：`#475569`×3 → `CANVAS_COLORS.labelTextLight`
- `DialMeter.tsx`：`#475569`×2 → `CANVAS_COLORS.labelTextLight`
- 组件内硬编码颜色违规归零

**验证**：`npx tsc --noEmit` 零错误

### AccelerationCenterExtra 规范违反修复（§3 完成）
**类型**：fix | **范围**：features/mechanics/kinematics

- `animate-bounce` ×2 → `animate-pulse`（铁律级违反）
- `bg-white rounded-xl shadow-sm` → `<Card className="p-2">`
- 全量 store 解构 → 7 个 `useAnimationStore((s) => s.xxx)` 精细订阅
- `#3B82F6` / `#EF4444` ×4 → `PHYSICS_COLORS.velocity` / `.acceleration`

**验证**：`npx tsc --noEmit` 零错误

### 全库 store selector 迁移（§6 完成）
**类型**：refactor | **范围**：67 个文件

- 67 个文件 67 处 `useAnimationStore()` 全量解构替换为 selector 模式
- 1-2 字段（8 文件）：逐个 `useAnimationStore((s) => s.xxx)` selector
- 3+ 字段（57 文件）：`useShallow` 浅比较，避免多余渲染
- actions 独立 selector（`setIsPlaying`、`setTime` 等稳定引用）
- 零逻辑变更，纯机械替换

**验证**：`npx tsc --noEmit` 零错误 · `npm run build` 成功

### TODO_deferred.md 精简
**类型**：docs | **范围**：docs

- 355 行 → 60 行：删除 §2-6（已完成/已关闭）+ 4 段历史记录
- 保留 6 个未完成项：§1(P0)、§7(P2)、§8(P2)、§9-11(P3)
- 新增 §8-11 颜色治理后续项（渐变对象化/SCENE_COLORS拆分/材质体系合并/命名统一）

## 2026-06-14

### 手指定则组件提取为通用组件
**类型**：refactor | **范围**：components, features, tests

- 将 `SkeletalHand` 和 `HandRule` 从 `features/electromagnetism/shared/` 提取到 `src/components/Physics/`，作为全局可复用的物理渲染组件
- `CuttingEMFHandRule` 移至 `src/features/electromagnetism/induction/CuttingEMFHandRule.tsx`（场景特定便捷工厂）
- 移动测试文件：`tests/features/electromagnetism/SkeletalHand.test.ts` → `tests/components/Physics/SkeletalHand.test.ts`
- 更新 `CuttingEMF.tsx` import 路径
- 删除 `src/features/electromagnetism/shared/` 整个目录

**涉及文件**：`src/components/Physics/SkeletalHand.tsx`(新建), `src/components/Physics/HandRule.tsx`(新建), `src/features/electromagnetism/induction/CuttingEMFHandRule.tsx`(新建), `src/features/electromagnetism/induction/CuttingEMF.tsx`(修改), `tests/components/Physics/SkeletalHand.test.ts`(移动), `src/features/electromagnetism/shared/`(删除)

**验证**：`npx tsc --noEmit` 零错误 · `npx vitest run` 255/255 通过

### 测试修复：forceMotion / SkeletalHand / physicsQuantities
**类型**：test | **范围**：tests

- 修复 3 个测试文件中共 28 个失败用例（→ 0 失败）：
  - `forceMotion.test.ts`：6/6 通过。API 重构 `scene`→`mode`，参数 `env`→`env1/env2/env3`，theta 修正，terminal velocity 改用 drag subMode。
  - `SkeletalHand.test.ts`：13/13 通过。骨骼数据从"等角手"改为"真人手"（thumb baseX -26→26, angle -130°→-50°），重写坐标断言。
  - `physicsQuantities.test.ts`：13/13 通过（原 23 个）。删除 11 个 label 已不存在的过时测试，更新 6 个 label 名称变更的测试。
- 测试总数：252→242（净减 10 个过时测试），全部通过。

**涉及文件**：`tests/physics/forceMotion.test.ts`, `tests/features/electromagnetism/SkeletalHand.test.ts`, `tests/data/physicsQuantities.test.ts`

**验证**：`npx vitest run` 242/242 通过。

### Lint errors 修复：11→0
**类型**：fix | **范围**：tests, src

- 修复全部 11 个 lint errors（warnings 70 个保持不变）：
  - 测试文件：删除 6 个未使用变量（`EPS`, `e`, `N`, `f_max`, `VariableMotionModel`, `r5`）。
  - `VTChart.tsx`：定义 `VTChartDataPoint` 接口，替换 4 处 `any`。
  - `FieldLines.tsx`：替换 2 处 `as any[]` 为具体类型（`{ cx, cy, r, opacity }[]` 和 `{ d, opacity }[]`）。
- 删除历史文档 `FIX_PLAN_eslint-registry.md`。

**涉及文件**：`tests/math/trigonometry.test.ts`, `tests/physics/celestial.test.ts`, `tests/physics/dynamics.test.ts`, `tests/physics/kinematics.test.ts`, `src/components/Physics/VTChart.tsx`, `src/features/electromagnetism/electrostatics/FieldLines.tsx`, `docs/agent-rules/process/FIX_PLAN_eslint-registry.md`（删除）

**验证**：`npm run lint` 0 errors / 70 warnings · `npm run build` 通过 · `npm test` 242/242 通过。

## 2026-06-13

### [M4] 电场线与等势面关系交互动画重构
**类型**：feature | **范围**：features, data

- 实现“电场线与等势面”三屏职责分离重构：
  - **左屏 Sidebar 交互控制**：新增 `FieldLinesSidebar.tsx` 控制组件，集成拓扑场景切换（单正电荷/单负电荷/等量异种/等量同种）、电场线及等势面显隐开关。
  - **中屏 SVG 动画与交互**：
    - 精准的物理计算：定义了微库仑级的电量和厘米级的空间尺寸作为物理尺度，映射到画布坐标；
    - 黄色电场线（`#EAB308`）：利用 Runge-Kutta 追踪场强线并采用平滑 SVG path 渲染，在长线上添加了均匀流向指示箭头；
    - 紫色等势面（`#A855F7`）：采用 Marching Squares 网格算法平滑插值生成等势面路径，并根据电势大小映射透明度，呈现由深至浅的精致同心圆/心形线网格；
    - 手持试探电荷探针：增加 Pointer 事件拖拽探针与把手，在探针处渲染橙色的电场力矢量箭头，并在探针拖动时将坐标与状态实时同步到 Store；
    - 能量守恒柱状图：在 SVG 画布上绝对定位悬浮半透明能量卡片，以双柱形式（青色动能 $E_k$，紫色势能 $E_p$）演示粒子沿等势线不做功、横穿场线此消彼长的守恒规律。
  - **右屏定量看板**：在 `electromagnetism.ts` 的 `anim-field-lines` 分支中实现绝对电势标量计算与路径积分电场力做功 $W_e = q(\varphi_{start} - \varphi_{current})$ 计算，补充高考考点及易错点分析。

**涉及文件**：[FieldLines.tsx](file:///d:/code/physic/physics-learning/src/features/electromagnetism/electrostatics/FieldLines.tsx), [FieldLinesSidebar.tsx](file:///d:/code/physic/physics-learning/src/features/electromagnetism/electrostatics/FieldLinesSidebar.tsx), [electromagnetism.ts](file:///d:/code/physic/physics-learning/src/data/quantities/electromagnetism.ts), [electromagnetism-electrostatics.ts](file:///d:/code/physic/physics-learning/src/data/registries/electromagnetism-electrostatics.ts)

**验证**：`npx tsc --noEmit` 静态类型检查通过。

### [M4] 平行板电容器交互动画优化
**类型**：feature | **范围**：features, data

- 实现接电源（U不变）与断电源（Q不变）双场景电路状态控制，支持 Toggle 插入介电常数 εᵣ=5 的电介质板（使用 CSS Transition 实现平滑滑动）。
- 重构中屏画布为**流式自适应布局**（彻底清除 Y 轴硬编码像素）：
  - **上图下动画**：顶部联动柱状图重构为独立 HTML 卡片，采用 Tailwind 和 Flex 实现响应式刻度、数值与渐变柱（高度百分比自适应）；底部模拟动画重构为独立 SVG 容器，极板与静电计垂直中线绑定到 `canvasSize.height / 2` 自适应居中，无死像素。
  - **悬浮状态卡片**：实验当前状态卡片改为 HTML 绝对定位浮动框，样式与 project UI 统一，解决极端参数下遮挡极板的隐患。
  - 极板上分布电荷粒子，粒子数量正比于当前电量 Q；电量变化时，在导线上动态展示电荷粒子回流/充电动画。
  - 极板间电场线数量和分布完全绑定极板电荷粒子，在断开电源拉大板距时表现为密度完全恒定，直观体现场强 E 与 d 无关的高考常考结论；在接通电源拉大板距时，电场线密度变稀疏。
  - 右侧绘制精致指针式静电计。**优化指针偏角灵敏度**：采用非线性幂函数映射（$U^{0.7}$），显著放大了中低电压段（特别是插入电介质电容变大、电压大幅降低时）的指针摆幅；且指针重构为通过 CSS `transform: rotate` 旋转，配合三阶贝塞尔过渡曲线，实现了极具物理真实阻尼感的顺滑表针摆动。
- 升级右侧物理看板数据构建器 `electromagnetism.ts`，充实 quantities、formulas、gaokaoPoints 以及 warnings，覆盖高考常考分析和误区警示。
- 注册 `CapacitorSidebar` 控制栏并简化 `paramMeta` 配置。

**涉及文件**：[Capacitor.tsx](file:///d:/code/physic/physics-learning/src/features/electromagnetism/electrostatics/Capacitor.tsx), [CapacitorSidebar.tsx](file:///d:/code/physic/physics-learning/src/features/electromagnetism/electrostatics/CapacitorSidebar.tsx), [electromagnetism.ts](file:///d:/code/physic/physics-learning/src/data/quantities/electromagnetism.ts), [electromagnetism-electrostatics.ts](file:///d:/code/physic/physics-learning/src/data/registries/electromagnetism-electrostatics.ts)

**验证**：`npx tsc --noEmit` 静态类型检查通过 · `npm run build` 成功执行并生成 Capacitor 懒加载 chunk。

## 2026-06-12

### [M2] 矢量箭头统一架构 — 基础模块 + CoulombLaw 迁移
**类型**：refactor | **范围**：utils, scene, components, theme, features, docs

- 重构 `vectorLength.ts`：签名改为 `(mag, type, maxLength, refMagnitude, minLength)`，引入 refMagnitude 归一化替代 `mag * sceneScale.scale` 量纲错误方案
- `VectorArrow.tsx` 新增 `refMagnitude?` prop，三层优先级链（单箭头覆盖 > 场景默认 > DEV warn）
- `SceneConfig.ts` / `SceneScale.ts` 新增 `refMagnitudes?: Partial<Record<VectorType, number>>`
- 迁移 CoulombLaw.tsx：移除 `pixelLength` 硬编码 override，BasicMode 用固定 `refMagnitude=50N`，ThreeChargeMode 动态计算 `maxForce * 1.2`
- 修复 ThreeChargeMode 矢量不可见 bug（vector 从单位方向向量改为实际力分量）
- 项目规范同步更新：`project_rules.md` / `ARCHITECTURE_RULES.md` 目录结构新增 `src/components/Physics/` 和 `src/scene/`；`07_CANVAS_SVG_CHART_RULES.md` 新增 §9 统一矢量渲染规范；`canvasStyle.ts VECTOR_DISPLAY` 标记 `@deprecated`

**涉及文件**：vectorLength.ts, VectorArrow.tsx, SceneConfig.ts, SceneScale.ts, CoulombLaw.tsx, project_rules.md, ARCHITECTURE_RULES.md, 07_CANVAS_SVG_CHART_RULES.md, canvasStyle.ts

**验证**：`npx tsc --noEmit` 零错误 · CoulombLaw 基础模式力矢量正常 · 三电荷模式力矢量正常

### [M2] 矢量箭头迁移 — 第 1 章运动学（5 文件）
**类型**：refactor | **范围**：features

- `VelocityAnimation.tsx`：2 marker → VectorDefs；2 箭头 → VectorArrow + refMagnitudes
- `VelocityAnimationStrip.tsx`：3 marker → VectorDefs；3 箭头 → VectorArrow + 动态 refMagnitudes（velocity/acceleration/displacement）；移除未使用 scale 变量
- `AccelerationAnimation.tsx`：3 marker → VectorDefs；3 箭头 → VectorArrow + refMagnitudes；移除 ARROW_SCALE_V/ARROW_SCALE_DELTA_V
- `AccelerationCenterExtra.tsx`：1 marker → VectorDefs；2 箭头 → VectorArrow + refMagnitudes；移除 SVG_MARKER import
- `UniformAccelerationAnimation.tsx`：3 marker → VectorDefs；2 箭头 → VectorArrow + refMagnitudes；曲线箭头保留 path + markerEnd（marker 引用改为 markerId()）

**涉及文件**：VelocityAnimation.tsx, VelocityAnimationStrip.tsx, AccelerationAnimation.tsx, AccelerationCenterExtra.tsx, UniformAccelerationAnimation.tsx

**验证**：`npx tsc --noEmit` 零错误 · 旧 marker 引用全部清除

## 2026-06-08

### [M1/M2] anim-satellite 人造卫星教学动画优化
**类型**：feature | **范围**：physics, data, features, tests

- 新增 calculateLaunchTrajectory 纯函数，支持坠毁/椭圆/逃逸轨道判定
- 重构右侧物理量看板，清除 Canvas 硬编码文本
- 新增双模式侧边栏与画中画 v(r)/T(r) 图表交互
- 极坐标时间积分推进，支持手势拖动卫星与图表反向驱动
- 补全天体物理单元测试（10 项），Vitest 100% 通过

**涉及文件**：celestial.ts, SatelliteAnimation.tsx, SatelliteSidebar.tsx, physicsQuantities.ts, animationRegistry.ts, celestial.test.ts

**验证**：`npx tsc --noEmit` + `vitest celestial.test.ts`

### [M1/M2] anim-gravity 万有引力教学动画优化
**类型**：feature | **范围**：data, features, tests

- 重构 anim-gravity 看板，支持相对单位比例验证和真实尺度探索双模式
- 新建 GravitySidebar 双模式切换组件
- 重构主画布：天体手势拖拽、画中画 F-r 曲线图（双向反向拖动交互）、引力场涟漪动效
- 100% 零硬编码色彩，复用 SCENE_COLORS.materials 和 PHYSICS_COLORS.gravity

**涉及文件**：GravityAnimation.tsx, GravitySidebar.tsx, physicsQuantities.ts, animationRegistry.ts, dynamics.test.ts

**验证**：`npx tsc --noEmit` + `vitest dynamics.test.ts`

### [M1/M2] anim-kepler 开普勒定律教学动画优化
**类型**：feature | **范围**：physics, data, features, tests

- 新增 solveKeplerEquation（牛顿迭代）和 calculateKeplerOrbit 纯函数
- 三大定律分步教学：第一定律双焦点验证、第二定律扫过面积、第三定律 T²-a³ 图表画中画
- 100% 零硬编码颜色，浅底对比度升级
- 编写 4 项单元测试（牛顿迭代收敛、焦半径恒定、角动量守恒）

**涉及文件**：celestial.ts, KeplerAnimation.tsx, KeplerSidebar.tsx, physicsQuantities.ts, animationRegistry.ts, celestial.test.ts

### [M2] 规范体系优化
**类型**：docs | **范围**：docs

- PROCESS_LOG.md 瘦身为索引文件，历史按周归档至 logs/
- 新增 process/CHECKLIST.md（完整提交流程检查项）
- 新增 07_CANVAS_SVG_CHART_RULES.md（动态布局、魔法数字判定、坐标分层、信息降级）
- 精简 project_rules.md（351→140 行），移除与子规范重复内容

**涉及文件**：PROCESS_LOG.md, CHECKLIST.md, 07_CANVAS_SVG_CHART_RULES.md, project_rules.md

## 2026-06-07

### [M1/M2] anim-centripetal 向心力与向心加速度优化
**类型**：feature | **范围**：features, data

- 新建 CentripetalSidebar（进阶模式 + F-a 联动图表开关）
- 自适应比例尺 + 矢量安全映射（max 75px），修复箭头飞出画布 Bug
- 右上角 F-a 图表卡片画中画，支持卡片反向交互
- 补全物理看板 quantities（基础/进阶双模式）

**涉及文件**：CentripetalSidebar.tsx, CentripetalAnimation.tsx, animationRegistry.ts, physicsQuantities.ts

### [M1/M2] anim-circular-motion 匀速圆周运动优化
**类型**：feature | **范围**：features, data

- 自适应比例尺替代硬编码 scale=50
- 角速度几何化（半透明弧形展示 θ=ωt）
- 进阶模式：正交投影联动 + 右上角 x-t/y-t 波形卡片画中画
- 物理看板精细化分档（基础/进阶）

**涉及文件**：CircularMotionSidebar.tsx, CircularMotionAnimation.tsx, animationRegistry.ts, physicsQuantities.ts

### [M1/M2] anim-oblique-throw 斜抛运动优化
**类型**：feature | **范围**：physics, features, data

- 新增 precomputeObliqueThrowWithDrag 纯函数
- 全宽铺满式物理演练 + 双轴自适应比例尺
- 双轴投影小球 + 右上角 v-t 图像联动画中画
- 零硬编码色彩

**涉及文件**：kinematics.ts, ObliqueThrowSidebar.tsx, ObliqueThrowAnimation.tsx, physicsQuantities.ts

### [M1/M2] anim-projectile 平抛运动优化
**类型**：feature | **范围**：physics, features, data

- 新增 precomputeProjectileWithDrag 纯函数
- 全宽铺满 + 自适应比例尺 + 双轴投影小球
- 右上角 v-t 图像联动画中画

**涉及文件**：kinematics.ts, ProjectileSidebar.tsx, ProjectileAnimation.tsx, physicsQuantities.ts

### [M1] anim-weightlessness N-a 图同步修复
**类型**：fix | **范围**：features, physics

- 修复暂停/播放结束后 N-a 图游标不跟随参数滑块 Bug
- 引入 actualA 与 currentA 分离
- 扩展 calculateElevatorMotion 支持 modelIdx=2（恒定加速度）
- 移除 physicsQuantities.ts 中 Canvas 布局依赖，提取 LAYOUT 常量

**涉及文件**：dynamics.ts, WeightlessnessAnimation.tsx, WeightlessnessCenterExtra.tsx, physicsQuantities.ts

### [M1] anim-connected-bodies 连接体问题修正
**类型**：refactor | **范围**：physics, features, utils

- 重写 calculateConnectedBody（修正只算 m₂ 摩擦力的物理错误）
- 新增 calculateConnectedBodyTimeline 纯函数
- 三处消费方统一调用，移除 Canvas 布局参数依赖
- 修正 T-μ/T-F 曲线，新增临界竖线
- 统一坐标换算与布局常量，事件监听器 cleanup

**涉及文件**：dynamics.ts, ConnectedBodiesAnimation.tsx, ConnectedBodiesCenterExtra.tsx, physicsQuantities.ts, coordinate.ts

### [M2] 死代码清理与预留函数规范化
**类型**：refactor | **范围**：utils, theme, physics, math

- 删除零引用文件：screenshot.ts, physicsColors.ts, format.ts
- 删除零消费桶文件：features/index.ts, mechanics/index.ts 等
- 预留物理/数学函数补 JSDoc + 参数单位 + @category M4
- 新建 tests/math/trigonometry.test.ts（26 个测试用例）

**涉及文件**：screenshot.ts, physicsColors.ts, format.ts, utils/index.ts, celestial.ts, energy.ts, electromagnetism.ts

### [M2] 规范偏离修复：未批准依赖卸载、裸调 rAF 消除、纯计算下沉
**类型**：compliance | **范围**：package, utils, physics, features

- 卸载 framer-motion 和 zod（零引用未批准依赖）
- 清理 motion.ts Framer Motion 专属预设
- 新增 useSimulationFrame Hook（统一 rAF 入口）
- 重构 useEquilibriumPhysics.ts，Euler-Cromer 积分下沉到 dynamics.ts

**涉及文件**：package.json, motion.ts, animation.ts, useEquilibriumPhysics.ts, dynamics.ts

### [M2] 全局色彩规划规范优化与硬编码颜色治理
**类型**：compliance | **范围**：theme, features, docs

- colors.ts 规范全学科物理量颜色映射（磁场绿、电流红、电场黄、重力深绿）
- sceneColors.ts 新增 COMMON_MATERIALS/ENVIRONMENT_COLORS/SPECIAL_EFFECTS/SAFETY_PRESETS/LAB_LABELS
- VerticalThrowAnimation/VelocityXTChart/VelocityVTChart 清除所有硬编码颜色
- 同步更新 project_rules.md 和 02_UI_RULES.md

**涉及文件**：colors.ts, sceneColors.ts, index.ts, VerticalThrowAnimation.tsx, VelocityXTChart.tsx, VelocityVTChart.tsx

### [M1/M2] anim-vector-addition 力的合成与分解重构
**类型**：feature | **范围**：physics, features, data

- 新增 calculateVectorAddition 和 calculateOrthogonalDecomposition 纯函数
- 新建 useVectorAdditionPhysics.ts 计算型 Hook
- 精密刻度坐标系 + 强交互手势拖动 + 精密吸附算法
- 补全 physicsQuantities case 和 animationRegistry 配置

**涉及文件**：dynamics.ts, useVectorAdditionPhysics.ts, VectorAdditionAnimation.tsx, physicsQuantities.ts, animationRegistry.ts

**验证**：`vitest dynamics.test.ts` + `npm run build`

### [M1/M2] anim-equilibrium 共点力平衡重构
**类型**：feature | **范围**：physics, features, data

- 新增 calculateEquilibriumTension 纯函数
- 新建 useEquilibriumPhysics.ts 计算型 Hook
- 精密实验仪视觉 + 直接拖动交互 + 断裂过载指示 + 三力封闭三角形
- 补全 physicsQuantities case 和 animationRegistry 配置

**涉及文件**：dynamics.ts, useEquilibriumPhysics.ts, EquilibriumAnimation.tsx, physicsQuantities.ts, animationRegistry.ts

**验证**：`vitest dynamics.test.ts` + `npm run build`

### [M1/M2] anim-newton-second 牛顿第二定律优化
**类型**：feature | **范围**：physics, features, data

- 新增 calculateNewtonSecondVariableMotion 纯函数（线性递增力/正弦力）
- 新建 NewtonSecondSidebar（普通/进阶模式切换）和 NewtonSecondCenterExtra（F-t/a-t/v-t 三曲线）
- 重构 Canvas 界面，不锈钢金属渐变材质，完善受力分析图

**涉及文件**：dynamics.ts, NewtonSecondAnimation.tsx, NewtonSecondSidebar.tsx, NewtonSecondCenterExtra.tsx, physicsQuantities.ts

### [M1/M2] anim-weightlessness 超重与失重重构
**类型**：feature | **范围**：physics, features, data

- 新增 calculateElevatorMotion 纯函数（电梯变速升降/钢索坠落缓冲）
- 精美体重秤 + 表盘指针偏角动态对齐 + 失重段砝码漂浮动效
- 新建 WeightlessnessSidebar 和 WeightlessnessCenterExtra（a-t/N-t/v-t 联动曲线）

**涉及文件**：dynamics.ts, WeightlessnessAnimation.tsx, WeightlessnessSidebar.tsx, WeightlessnessCenterExtra.tsx, physicsQuantities.ts

## 2026-06-06

### [M1/M2] anim-gravity-basic 重力与重心教学演示
**类型**：feature | **范围**：physics, features, data

- 新增 calculateEarthGravity 纯函数
- 模式一：地球自转重力分解（万有引力/向心力/重力三力分解矢量）
- 模式二：悬挂法重心实验（阻尼正弦衰减晃动 + 配重滑块 + 质心公式）
- 新建 GravityBasicSidebar，注册 anim-gravity-basic，补全 physicsQuantities case

**涉及文件**：dynamics.ts, GravityBasicAnimation.tsx, GravityBasicSidebar.tsx, animationRegistry.ts, physicsQuantities.ts, knowledgeTree.ts

**验证**：`vitest dynamics.test.ts` + `npm run build`

### [M1/M2] anim-friction 摩擦力双模式重构
**类型**：feature | **范围**：physics, features, data

- 新增 calculateFrictionPullModel 和 calculateFrictionInclineModel 纯函数
- 模式一：水平拉力（金属滑轨+木箱+阻力灰尘粒子）
- 模式二：斜面倾斜（旋转矩阵精确绘制力矢量）
- 新建 FrictionSidebar + FrictionCenterExtra（f-F/f-θ 图表）

**涉及文件**：dynamics.ts, FrictionAnimation.tsx, FrictionCenterExtra.tsx, FrictionSidebar.tsx, animationRegistry.ts, physicsQuantities.ts

**验证**：`vitest dynamics.test.ts` + `npm run build`

### [M1/M2] 重力与弹力教学改进
**类型**：feature | **范围**：features, data

- physicsQuantities.ts 增加 anim-gravity 和 anim-spring-force case
- 新建 SpringForceCenterExtra（F-x 图像 + 弹性势能三角形阴影）
- SpringForceAnimation 重构为三维螺旋线投影 + 形变量尺寸线
- GravityAnimation 重构为金属径向渐变球体 + 引力场涟漪

**涉及文件**：SpringForceCenterExtra.tsx, SpringForceAnimation.tsx, GravityAnimation.tsx, physicsQuantities.ts

### [M1/M2] anim-vertical-throw 竖直上抛运动优化
**类型**：feature | **范围**：physics, features

- 升级 precomputeVerticalThrowTrajectory（真空对比点集）
- 真空双轨对比 + 实验设备材质美化
- 最高点认知强化（v=0 自动暂停 + 悬浮科学看板）
- v-t 图积分面积着色 + 时间游标

**涉及文件**：kinematics.ts, VerticalThrowSidebar.tsx, VerticalThrowAnimation.tsx

**验证**：`vitest kinematics.test.ts`

### [M1/M2] anim-velocity 速度概念教学重构
**类型**：feature | **范围**：physics, features, data

- 新增 precomputeVariableMotion 纯函数 + useVelocityPhysics Hook（O(1) 插值）
- 基础版视觉美化（公交车/运动员/打点轨迹）
- 进阶版 VelocityAnimationStrip（气垫导轨/立体弹簧/滑块小车）
- VelocityXTChart/VelocityVTChart 重构（科学网格 + 积分面积 + 放大镜偏光镜）

**涉及文件**：kinematics.ts, useVelocityPhysics.ts, VelocityAnimation.tsx, VelocityAnimationStrip.tsx, VelocityXTChart.tsx, VelocityVTChart.tsx

**验证**：`vitest kinematics.test.ts`（30 项通过）

### [M1/M2] anim-free-fall 自由落体重构优化
**类型**：feature | **范围**：physics, features

- 下沉 Somigliana 纬度/海拔重力修正公式
- 新增 precomputeFreeFallWithDrag 纯函数
- 重构 useFreeFallPhysics（O(1) 离线轨迹插值）
- 牛顿管视觉升级（晶莹立体玻璃气缸 + 科学标尺网格）
- 水滴动画重构（撕裂形变 + 抛物线物理飞射轨迹）

**涉及文件**：kinematics.ts, useFreeFallPhysics.ts, FreeFallAnimation.tsx, FreeFallDripAnimation.tsx

**验证**：`vitest kinematics.test.ts`（27 项通过）

## 2026-06-05

### [M1] anim-velocity 速度动画进阶版重构
**类型**：feature | **范围**：physics, features, data

- 新增 5 个纯函数（calculateAverageVelocity/VariableAcceleration/SecantSlope/TangentSlope/InstantaneousVelocity）
- 新建 VelocityCenterExtra/VelocityXTChart/VelocityVTChart/VelocityAnimationStrip
- physicsQuantities anim-velocity 分支重写（基础版+进阶版）
- animationRegistry 扩展进阶版参数（15 个）
- 修复 7 个 Bug（进阶版崩溃/非响应式/SHM 范围/曲线颜色/时间不重置等）

**涉及文件**：kinematics.ts, VelocityCenterExtra.tsx, VelocityXTChart.tsx, VelocityVTChart.tsx, VelocityAnimationStrip.tsx, physicsQuantities.ts, animationRegistry.ts

**验证**：`vitest kinematics.test.ts`（23 项新建）

### [M1] anim-vertical-throw 竖直上抛三屏联动重构
**类型**：feature | **范围**：features, data

- 完全重写为中屏双核布局（物理演练区 + 图象联动区）
- 新建 VerticalThrowSidebar（进阶模式 + 微元切片/空气阻力/目标高度线）
- 进阶功能：微元切片、空气阻力、目标高度双解、面积数值、时间悬标拖拽
- types.ts 新增 ParamMeta showIf 字段

  **涉及文件**：VerticalThrowAnimation.tsx, VerticalThrowSidebar.tsx, animationRegistry.ts, types.ts, kinematics.ts, physicsQuantities.ts

### [M4] anim-faraday-law 法拉第电磁感应定律重构
**类型**：feature | **范围**：physics, features, data, tests

- 新增 `computeFaradayEmf`、`computeFaradayMagnetFlux` 物理纯函数
- 新增 `generateUniformFaradayPoints` 和 `generateMagnetFaradayPoints` 图表解析式生成纯函数，支持 O(1) 瞬时图表更新
- 重构 `FaradayLaw.tsx` 为中屏左右动态分区布局，实现拟物仿真区与解析曲线看板联动
- 基础模式支持磁铁匀速往返运动、自由电子随电流流向漂移；进阶模式提供变密疏 ⊗/⊙ 磁感线点阵以及线圈黄色环绕光晕电流绕行光点
- 新增 `FaradaySidebarExtra.tsx` 页面控制侧栏，完美适配 `useAnimationStore` 状态机与 SegmentedControl
- 补全 `physicsQuantities.ts` 的 quantities/formulas/gaokaoPoints 右屏数据回显

**涉及文件**：electromagnetism.ts, electromagnetism-induction.ts, FaradayLaw.tsx, FaradaySidebarExtra.tsx, electromagnetism.ts (quantities), electromagnetism.test.ts

**验证**：`vitest electromagnetism.test.ts`
