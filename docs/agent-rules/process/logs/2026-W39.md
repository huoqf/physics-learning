# 2026-W39 工程日志（09-20 ~ ）

## 审查报告 P0 修复批次（09-22 完成）

**背景**：对项目做了一轮只读审查（高中物理内容符合性 + 中屏动画物理正确性 + 架构），产出《物理学习项目审查报告》并逐条核对。本批按**确认范围**施工：**物理错误 F1–F7 + 归类/文案 R1/R3/R4/R5/R6 + 规范违规 A7**。**未**触碰 P0-1（依赖环）、P0-2（544 处 `??` 兜底）等架构大改。

### 一、七条物理错误（F1–F7）

| 编号 | 问题 | 修复 |
|---|---|---|
| **F1** | 折射基础模式角度从**界面**而非法线量起，画成"疏→密却远离法线"；进阶模式却是对的 | 入射/折射方向改 `(cosθ, sinθ)`；角度弧改沿真实光线绘制；进阶模式弧同步纠正 |
| **F2** | 全反射折射光被画进水里（`refrDirY=+cosθ₂`） | 改 `-cosθ₂`，折射光射向**上方空气**；入射光线起点一并纠正 |
| **F3** | 全反射反射方向错误，射向空气侧 | 改 `reflectedDirX = incidentDirX; reflectedDirY = -incidentDirY`（保持 x、翻转 y）；角度弧重画 |
| **F4** | 玻尔轨道半径画成 r∝n，应 r∝n² | `baseR` 按 `n²` 计算；跃迁箭头、轨道电子同步 |
| **F5** | 静摩擦凭空乘 `1.12`，导致两模块临界角不一致（16.7° vs 18.6°） | 删除系数，统一 `tanθ_c = μ`；补单测回归保护（断言 ≠ `atan(1.12μ)`） |
| **F6** | 热力学沙箱 W/V/p 互不自洽（标称 100 J、物理上 ≈11 J，差一个数量级） | 重写为**等容传热 + 绝热做功**两段过程：`T₁=T₀+Q/C`、`V=V₀(T₁/T)^(1/(γ−1))`、`P=T/(3V)`；∫p dV 与标称功严格自洽；体积→活塞高度收敛为**单一映射** `calculatePistonY(V)` |
| **F7** | H-α 谱线（1.89 eV / 656 nm，可见红光）被标成红外 | 新增 `photonVisibleRed` 语义色，`'3->2'` 改用之；`photonInfrared` 调暗表示"不可见" |

### 二、归类与文案（R1/R3/R4/R5/R6）

- **R1** `quantities/thinLens.ts`：共轭法测焦距 `importance: 'gaokao'` → `'extend'`。
- **R3** `quantities/polarization.ts`：布儒斯特角 `'gaokao'` → `'extend'`。
- **R4** `registries/mechanics-dynamics.ts`：统一 `omegaScale` 标签为"自转向心力放大倍数"，删除重复的"离心力放大倍数" controlMeta，tip 改为向心力表述（教材口径）。
- **R5** `physics/collision.ts` + `registries/mechanics-momentum.ts`：命名 "能量损失系数 k_loss" → "非弹性程度 k = 1 − 恢复系数 e"。
- **R6** `quantities/refraction.ts`：删除空气→玻璃"可能全反射"的**不可达**告警分支。
- **R2 撤回**：审查建议的 `L ≥ 4f` **有误** —— 共轭法必须 `L > 4f`（`L = 4f` 时两次成像重合），仅补一句消歧义，未按审查建议改。

### 三、规范违规（A7）

- `utils/animation.ts` 新增 `scheduleFrame(cb)` 一次性帧调度出口（项目内**唯一**允许调用 rAF 之处）；`utils/useCanvasSize.ts` 改走该出口 → `check-no-raf` 通过。
- `scripts/legacy-marker-whitelist.json`：3 项过期白名单延期至 `2026-12-31`，附 `_reviewNote`（VectorDefs 暂无法覆盖非箭头 marker）。

### 四、验证（全绿）

```bash
node ./node_modules/typescript/bin/tsc --noEmit   # 0 error
node ./node_modules/vitest/vitest.mjs run          # 56 files / 821 tests passed
node scripts/check-no-raf.mjs                      # pass
node scripts/check-no-marker.mjs                   # pass
node scripts/check-font-size.mjs / check-large-files.mjs / check-viewport-legacy.mjs  # pass
node ./node_modules/vite/bin/vite.js build         # ✓ built in 34.98s, 2372 modules
```

### 五、审查报告修正与存档

逐条复核后订正报告中的**误报 / 计数 / 建议**：

- **删除误报 G2** —— `prob-2024-quanguo-21` 定义在 `problems/mechanics/prob-2024-quanguo-21.ts`，由 `problems/index.ts` 汇入，**并未悬空**；
- **订正 G4** —— 实验动画数 1 → **2**（含 `anim-experiment-er` 测定电源电动势与内阻），处置建议由"新建 4 个实验"改为"**挂载既有 + 补 `experiment-1-2` 空 `animationIds` + 误差分析深化**"；
- **撤回 R2**；**替换 4B.3 论据**（`intentionalNonUniformScale` 实为 4 处使用，零使用的是 `physicsScaleDesignX/Y`）；
- **修正 §五「g 无散落硬编码」**（实为 40+ 处 `?? 9.8` 兜底，应并入 P0-2 治理）；
- 订正 **G1 年份分布 / A6 导出符号数 / A9 计数 / P0-1 断环残留（26 节点环）**；修正 **F3 字面表述**。

修正版（v4）归档至 **`docs/reports/physics-learning-审查报告-2026-09-22.md`**。

---

## 电磁振荡与电磁波模块上线 + 六轮审查闭环 + 组件库规范收口（09-24 完成）

**背景**：按《内容补全建议》**批次 1** 施工 `electricity-6-1/6-2/6-3`；随后连续 **6 轮**只读审查（"页面优化是否符合高中物理" → "问题修复核对" → 反复"重新检查"），闭环全部 P0/P1/P2 与部分 P3，并顺带收口组件库规范。

### 一、新增章节：电磁振荡与电磁波 🎉（`29c6033` 10:10）

- 新增 3 个知识节点 `electricity-6-1`（LC 振荡）/ `6-2`（麦克斯韦电磁场理论与电磁波）/ `6-3`（电磁波谱与无线电波），
  三者共用薄壳 `EMOscillationAnimation`，由 `params.scene` 分派到 3 个独立场景组件（`LCOscillationScene` / `EMWaveScene` / `EMSpectrumScene`）。
- **硬约束遵守**：**阻尼定性**——仅通过能量衰减包络 + 文字提示呈现，**未解二阶 RLC 微分方程**；画布 `CANVAS_PRESETS.splitV`（840×325）；
  SVG 内**零整段教学说明**（说明全部落在右屏 `PhysicsPanel` / `CenterExtra`）。
- 组件复用（未手写等效实现）：`CapacitorPlates` / `CoilBase` / `Solenoid` / `DialMeter` / `MeterPointer` / `EnergyBars` / `VectorArrow` / `BasePhysicsChart`。
- 配套：`src/physics/lcOscillation.ts` + `src/physics/emWave.ts`（`λ = c/f`，`SPEED_OF_LIGHT = 3.0e8`）、`src/data/quantities/emOscillation.ts`、
  registry `electromagnetism-em-oscillation.ts`、`physicsQuantities.ts` 补齐 quantity entry（**0 悬空 0 孤儿**）。

### 二、规范与门禁收口（`4f17dc2` 10:20）

- **SKILL 新增 G 节「物理保真度与教学规范（按需查阅资源库）」** + 2 个新 reference：
  - `.agents/skills/refactor-animation-page/references/PHYSICAL_RULES.md`（42 行）：矢量坐标系陷阱（`VectorArrow.vector` 为物理坐标 / `originDesign` 为设计坐标）、
    闭合回路环流全链路验算、绕组箭头须落正面可见半环、**严禁指标过拟合式假同相**（点名 `min(..., lambda * k)`）、三维正交透视三件套、SSOT、注册表一致性。
  - `.agents/skills/refactor-animation-page/references/GAOKAO_STANDARDS.md`（35 行）：**严禁 THz**、微观尺度参照物对应表、禁裸科学记数法、浮点残差零截断 `1e-4`、小数位平滑、`999.5` 升档临界点。
- **新增第 6 条守门脚本** `scripts/check-component-reuse.mjs`（151 行），三类检查：
  ① `[unexported-component]` 公共目录 PascalCase 组件须在 barrel `index.ts` 导出；② `[unindexed-component]` 须在 `COMPONENT_REGISTRY.md` 登记；
  ③ `[shadowed-component]` 禁止在 `src/features/` 私造与核心物理组件同名的实现。
  接入 `npm run check:architecture`（5 → **6** 条）。
- `COMPONENT_REGISTRY.md` 补登记 11 个未登记组件（`SolenoidFieldLines` / `CoupledCoilField` / `MagneticPoles` / `MagneticFieldGrid` /
  `ParametricMagneticField` / `Rails` / `LightBulb` / `Rheostat` / `RotatingCoil` / `TransformerApparatus` / `DragHandle`）。

> ⚠️ **门禁有效性已实测**：在 `/tmp` 造三类违规样本 → 退出码 1，三个错误前缀全部命中（**不是永远绿的摆设**）。
> ⚠️ **但粒度只到「名字」**：`check-component-reuse.mjs:82` 仅 `registryContent.includes(compName)`，**不校验 props 签名** —— 见下方 §四 的 P1-14。

### 三、`em-wave` 修复（`fada08f` 10:30 · `810c009` 14:08）

| 提交 | 内容 |
|---|---|
| `fada08f` | **解耦同相贯穿虚线**：`EMWaveScene` 新增 `useMemo` 版 `peakSamples`（按 `\|y\| > 0.75` 提局部极值，>10 个隔一取样），从 `combs` 门禁拆出 ⇒ **全频段生效**；**清除 THz 课标外单位**（`formatFrequency` 删 THz 分支，≥999.5 GHz 走科学记数法）；`formatLCEnergy` 补 30 行边界单测 |
| `810c009` | **组件登记表接口纠正**（14 行 props）+ **`Spring` 彻底归位**：`git mv src/components/UI/Spring.tsx → src/components/Physics/Spring.tsx`，13 处调用点改 `@/components/Physics`，`UI/index.ts` 删除导出，`02_UI_RULES.md` 的 `Spring` 行从 UI 表移入 §7.1 Physics 表 |

### 四、六轮审查闭环明细

**已闭环**：

| 级 | 项 |
|---|---|
| P0 | **P0-A** `colors.white` 不存在（阻断 build）→ 改 `CANVAS_COLORS.white`；**P0-B** `COIL_WIDTH` 死常量（TS6133）→ 删除 |
| P1 | P1-4 电流箭头未成闭合环流；P1-5 相位漂移（**部分**，见 §六）；P1-6 绕组箭头画在远端半环；P1-7 能量读数裸 `toPrecision` → 新增物理层唯一入口 `formatLCEnergy` |
| P2 | P2-5 硬编码 `#ffffff`；P2-6 游标 `-0.00`；P2-7 `ANNOTATIONS.md` Scene 列写错；**P2-8 可见光用 THz**；P2-9 尺度参照与卡片 λ 差 4–10 倍；**P2-10 首帧闪英文参数转储** |
| P3 | P3-6 `lambdaMax` JSDoc 漂移；P3-8 阻尼段因果措辞；P3-11 能量位数跳变；P3-13 `formatLCEnergy` 零单测 |
| 规范 | **P1-14** 组件登记表新增 11 条 props **11/11 全错** → 已修正 14 行；`Spring` 组件归属（`UI/` vs `Physics/`，两份文档互相矛盾）→ 已归位 |

**实机复验关键数据**（Playwright，`localhost:5173`）：

| 项 | 结果 |
|---|---|
| P3-11 能量位数 | `t = 1.58 s` → 右屏 `Ee = 0.00 J`（此前 `0.0000 J`） |
| P2-8 | `anim-em-spectrum` 可见谱段 `innerText` 中 **THz 0 命中**，显示 `5.45 × 10¹⁴ Hz` |
| P1-5 贯通虚线 | f = 100/200/300/500/700/1000 MHz → **2 / 4 / 6 / 10 / 7 / 10 条**（修复前 f ≥ 500 MHz 全为 **0** 条） |
| 磁学链条 | 左 N / 右 S ↔ 内部箭头 S→N ↔ 外部箭头 N→S ↔ 绕组箭头上行 ↔ `isLeftNorth`，独立右手定则验算 `dB = dl × r̂ = ŷ × (−ẑ) = −x̂` ⇒ N 在左 ✅ |

### 五、验证（全绿）

```bash
npx tsc -b --force                                  # 0 error（强制全量重建）
npx eslint . --max-warnings 0                       # 0 error 0 warning
npm run check:architecture                          # 6/6 通过（含新增 check:component-reuse）
npx vitest run                                      # 60 files / 892 tests passed（888 → +4）
```

### 六、遗留项

**已全部登记到 `process/TODO_deferred.md`**：

- **第十章**（页面审查遗留技术债）：§10.1 E/B 表观相位差仍 ∝ f（1000 MHz 达 104.1°）；§10.2 `SolenoidFieldLines` vs `CoupledCoilField` 双实现 + 右手定则判据双实现；
  §10.3 物理层零截断阈值不统一（`1e-12`/`1e-14` vs `1e-4`）；§10.4 `LC_DEFAULTS` 偏慢（T = 6.28 s）；§10.5 守门脚本粒度不足；§10.6 登记表 props 漂移风险。
- **第九章**（内容补全待办）：16 个缺口考点仅完成 3 项；3 个横向基座未建；实验专题 9 个节点未建；2025 真题 0 条。

### 七、审查报告归档

六轮审查报告 **`physics-learning-页面优化高中物理符合性审查-2026-09-24.md`（§1–§14，1133 行）**，
新增章节：§9 第三轮核查 · §10 第四轮修复核对 · §11 第五轮修复核对 · §12 第五轮续（门禁验证 + P1-14 新发现）· §13 第六轮（未提交改动核对 + `Spring` 归属根因）· §14 重新检查 + 内容补全进度盘点。

> **待归档**：该报告当前位于工作区，尚未复制到 `docs/reports/`（本次审查由外部会话产出）。

---

## 缺口考点批次 1 施工上线（09-24 完成）

**范围**：
1. `electricity-5-5`：电感与电容对交变电流的作用（感抗与容抗）
2. `vibration-2-3`：多普勒效应

**主要交付**：
1. **纯物理计算**：
   - `src/physics/electromagnetism/acImpedance.ts` + `acImpedance.test.ts`（感抗、容抗、阻抗与有效值功率）
   - `src/physics/vibration/doppler.ts` + `doppler.test.ts`（视在波长、视在频率与偏心圆波阵面扩散几何）
2. **splitV 分屏动画实现（上图表下场景）**：
   - `ACImpedanceAnimation`：上方频响特性图（$X_L(f)$ 线性线与 $X_C(f)$ 双曲线关系图 + 谐振点游标），下方实验电路台（复用 LightBulb、DialMeter、CoilBase、CapacitorPlates，人教版控制变量法基准）。
   - `DopplerAnimation`：上方三通道接收示波器时域波形图，下方波源水平巡航偏心波阵面扩散空间场（声速比严格保真，波前圆心与波源轨迹实时严丝合缝对齐）。
3. **数据注册闭环**：
   - 知识树追加两个节点；
   - 注册表追加 `anim-ac-lc-impedance` 与 `anim-doppler-effect` 及高考真题预设；
   - 物理看板 quantities 构建器与懒加载注册；
   - `ANIMATION_COUNT` 105 → 107 并同步测试守卫。
4. **门禁验证**：
   - `tsc --noEmit` 0 error；
   - `npm run check:architecture` 6/6 全绿；
   - `npx vitest run` 62 文件 / 901 单测全部通过；
   - `npx eslint` 0 error 0 warning。
