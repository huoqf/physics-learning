# 2026-W39 工程日志（09-20 ~ ）

## 审查报告 P0 修复批次（09-22 完成）

**背景**：对项目做了一轮只读审查（高中物理内容符合性 + 中屏动画物理正确性 + 架构），产出《物理学习项目审查报告》并逐条核对。本批按**确认范围**施工：**物理错误 F1–F7 + 归类/文案 R1/R3/R4/R5/R6 + 规范违规 A7**。**未**触碰 P0-1（依赖环）、P0-2（544 处 `??` 兜底）等架构大改。

### 一、七条物理错误（F1–F7）

| 编号 | 问题 | 修复 |
|---|---|---|
| **F1** | 折射基础模式角度从**界面**而非法线量起，画成"疏→密却远离法线"；进阶模式却是对的 | 入射/折射方向改 `(cosθ, sinθ)`；角度弧改沿真实光线绘制；进阶模式弧同步纠正 |
| **F2** | 全反射折射光被画进水里（`refrDirY=+cosθ₂`） | 改 `-cosθ₂`，折射光射向**上方空气**；入射光线起点一并纠正 |
| **F3** | 全反射反射方向错误，射向空气侧 | 改 `reflectedDirX = incidentDirX; reflectedDirY = -incidentDirY`（保持 x、翻转 y）；角度弧重画 |
| **F4** | 玻尔轨道半径画成 r∝n，应 r∝n² | `baseR` 按 `n²` 计算；跃迁箭头、轨道电子同步 |
| **F5** | 静摩擦凭空乘 `1.12`，导致两模块临界角不一致（16.7° vs 18.6°） | 删除系数，统一 `tanθ_c = μ`；补单测回归保护（断言 ≠ `atan(1.12μ)`） |
| **F6** | 热力学沙箱 W/V/p 互不自洽（标称 100 J、物理上 ≈11 J，差一个数量级） | 重写为**等容传热 + 绝热做功**两段过程：`T₁=T₀+Q/C`、`V=V₀(T₁/T)^(1/(γ−1))`、`P=T/(3V)`；∫p dV 与标称功严格自洽；体积→活塞高度收敛为**单一映射** `calculatePistonY(V)` |
| **F7** | H-α 谱线（1.89 eV / 656 nm，可见红光）被标成红外 | 新增 `photonVisibleRed` 语义色，`'3->2'` 改用之；`photonInfrared` 调暗表示"不可见" |

### 二、归类与文案（R1/R3/R4/R5/R6）

- **R1** `quantities/thinLens.ts`：共轭法测焦距 `importance: 'gaokao'` → `'extend'`。
- **R3** `quantities/polarization.ts`：布儒斯特角 `'gaokao'` → `'extend'`。
- **R4** `registries/mechanics-dynamics.ts`：统一 `omegaScale` 标签为"自转向心力放大倍数"，删除重复的"离心力放大倍数" controlMeta，tip 改为向心力表述（教材口径）。
- **R5** `physics/collision.ts` + `registries/mechanics-momentum.ts`：命名 "能量损失系数 k_loss" → "非弹性程度 k = 1 − 恢复系数 e"。
- **R6** `quantities/refraction.ts`：删除空气→玻璃"可能全反射"的**不可达**告警分支。
- **R2 撤回**：审查建议的 `L ≥ 4f` **有误** —— 共轭法必须 `L > 4f`（`L = 4f` 时两次成像重合），仅补一句消歧义，未按审查建议改。

### 三、规范违规（A7）

- `utils/animation.ts` 新增 `scheduleFrame(cb)` 一次性帧调度出口（项目内**唯一**允许调用 rAF 之处）；`utils/useCanvasSize.ts` 改走该出口 → `check-no-raf` 通过。
- `scripts/legacy-marker-whitelist.json`：3 项过期白名单延期至 `2026-12-31`，附 `_reviewNote`（VectorDefs 暂无法覆盖非箭头 marker）。

### 四、验证（全绿）

```bash
node ./node_modules/typescript/bin/tsc --noEmit   # 0 error
node ./node_modules/vitest/vitest.mjs run          # 56 files / 821 tests passed
node scripts/check-no-raf.mjs                      # pass
node scripts/check-no-marker.mjs                   # pass
node scripts/check-font-size.mjs / check-large-files.mjs / check-viewport-legacy.mjs  # pass
node ./node_modules/vite/bin/vite.js build         # ✓ built in 34.98s, 2372 modules
```

### 五、审查报告修正与存档

逐条复核后订正报告中的**误报 / 计数 / 建议**：

- **删除误报 G2** —— `prob-2024-quanguo-21` 定义在 `problems/mechanics/prob-2024-quanguo-21.ts`，由 `problems/index.ts` 汇入，**并未悬空**；
- **订正 G4** —— 实验动画数 1 → **2**（含 `anim-experiment-er` 测定电源电动势与内阻），处置建议由"新建 4 个实验"改为"**挂载既有 + 补 `experiment-1-2` 空 `animationIds` + 误差分析深化**"；
- **撤回 R2**；**替换 4B.3 论据**（`intentionalNonUniformScale` 实为 4 处使用，零使用的是 `physicsScaleDesignX/Y`）；
- **修正 §五「g 无散落硬编码」**（实为 40+ 处 `?? 9.8` 兜底，应并入 P0-2 治理）；
- 订正 **G1 年份分布 / A6 导出符号数 / A9 计数 / P0-1 断环残留（26 节点环）**；修正 **F3 字面表述**。

修正版（v4）归档至 **`docs/reports/physics-learning-审查报告-2026-09-22.md`**。
