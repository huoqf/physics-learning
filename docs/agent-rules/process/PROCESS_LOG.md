# 物理演示项目工程日志 (PROCESS_LOG)

## 2026-06-02
- **任务目标**：修正电磁学“右手定则”可视化演示中的手性认知偏差。
- **实施内容**：
    1.  **架构重构**：废弃了 `SkeletalHand.tsx` 中基于 `mirrorX` 的对称几何镜像逻辑，改为解耦的独立手性骨骼模型体系。
    2.  **视觉修正**：在 `Palm` 组件中移除了通用纹理，改为根据物理驱动的状态 (`isBack`) 动态渲染掌心/手背文字标注。
    3.  **逻辑对齐**：根据“磁感线穿过手心”的物理规则，将 `CuttingEMF` 页面中的磁场方向 (`B_out`) 与手掌渲染状态进行了精准映射（向里$\otimes$$\rightarrow$掌心，向外$\odot$$\rightarrow$手背），确保物理演示的严谨性。
    4.  **架构升级**：更新了 `SkeletonHand` 组件的 props 接口，引入 `isBack` 属性实现物理层驱动视觉渲染的无状态架构。

## 2026-06-02
- **任务目标**：修复右手定则演示中手部姿态偏斜问题。
- **实施内容**：
    1.  重构 `computeHandPose` 算法，切换旋转参考系。
    2.  从“以拇指为基准对齐”更改为“以四指平面（中指指向）为基准对齐”。
    3.  解决了人体解剖学中拇指与四指夹角引起的视觉扭曲问题。
    4.  验证了该修正对 `chirality` (手性) 判定逻辑的物理无损性。
