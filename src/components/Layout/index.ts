/**
 * 布局组件库
 *
 * 提供项目中所有页面布局组件，包括三栏布局、网格布局等。
 * 所有布局组件支持响应式设计，适配不同屏幕尺寸。
 *
 * @example
 * ```tsx
 * import { ThreePanel } from '@/components/Layout'
 *
 * // 使用三栏布局
 * <ThreePanel
 *   left={<ParamPanel />}
 *   center={<PhysicsCanvas />}
 *   right={<InfoPanel />}
 * />
 * ```
 */
export { ThreePanel } from './ThreePanel'
